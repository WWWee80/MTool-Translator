
## v1.1.10 更新说明

### 修复
- API 返回 429 限流时自动退避重试（1.5s→3s 自适应），不再直接判为失败
- 大批量失败进入低并发重试前，先等待 30 秒限流窗口过去，避免重试风暴全部再次失败
- 着色器代码/CSS/二进制乱码原文被返回时不再误判为"翻译失败"（不计失败、不重试、不浪费额度）
- 进度文件中"结果=原文"的乱码条目重新加载后不再反复回炉重翻
- 语言代码规范化（zh-CN→zh-Hans 等映射），提升部分引擎翻译质量
- 批量翻译结果解析容错增强，减少偶发解析报错
- 连接测试失败时显示具体错误原因，方便排查

### 新增
- DeepSeek Prompt 缓存体系：固定公共前缀复用 + 缓存预热 + 命中率监控 + 费用估算，大幅降低 token 消耗
- embedding 智能免翻（可选）：本地 Ollama 语义判断资源名/代码/内部标识符，翻译前自动跳过
- Unity 游戏导出文本专属支持：自动识别并启用乱码/资源名过滤模式
- glossary 术语表：强制约束专有名词翻译一致性
- 双缓存断点续翻：进度文件 + 翻译缓存双写，防止进度损坏
- GUI 冻结看门狗：界面无响应自动检测提示

### 优化
- 翻译前噪声过滤大幅扩展：二进制乱码（U1~U8 规则）、短中文乱码、韩文乱码、路径与资源名
- 精修/校准流水线强化：新增"校准→精修"两阶段模式与精修泄漏清理
- 翻译不全检测增强
- 双引擎并发稳定性提升；Google 免费引擎代理池自动剔除失效节点
- 语义问题扫描重写，检查更快、误报更少

### 下载
- **MTool翻译工具.exe** - Windows单文件版，双击运行，无需安装Python
- **MTool翻译工具.py** - 源码版，需自备 Python 3.10+ 环境

## v1.1.6 更新说明

### 修复
- 修复Ollama本地模型翻译进度不更新的问题（返回原文被误判为失败）
- 修复非精修模式下engine_name变量未定义导致的崩溃
- 修复双引擎模式下进度不更新的问题

### 优化
- 去掉"返回原文=失败"判断，模型返回非空即视为成功
- 优化_should_skip_translation函数，只跳过明显不需要翻译的文本（纯数字、URL、路径、邮箱、占位符），不再跳过短英文单词和代码标识符
- Ollama本地模型禁用批量翻译，避免卡住
- 去掉自适应背压机制，用简单指数退避重试

### 新增
- 必应翻译(免费)引擎
- 代理设置（Cloudflare Worker代理，防限流）
- 引擎建议并发数（选择引擎后自动填入）

### 下载
- **MTool翻译工具.exe** - Windows单文件版，双击运行，无需安装Python
